# `$unset` merge directive

> **Status:** Superseded by `directive-design.md` · _last reviewed 2026-06-29_
>
> The final `$unset` design and implementation record live in `directive-design.md`; the code is in `crates/tailor-config/src/merge.rs`. This file is retained only as the earlier standalone proposal.

